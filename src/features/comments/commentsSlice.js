import { createSlice} from "@reduxjs/toolkit";

const creatSlice = {
    data: [],
    status: 'idle'
}

const commentsSlice = creatSlice({
    name: 'comments',
    initialState,
    redusers: {}
})

export default commentsSlice.reduser;